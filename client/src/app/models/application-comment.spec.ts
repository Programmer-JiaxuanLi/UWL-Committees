import { ApplicationComment } from './application-comment';

describe('ApplicationComment', () => {
  it('should create an instance', () => {
    expect(new ApplicationComment()).toBeTruthy();
  });
});
